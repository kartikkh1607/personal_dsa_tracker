node_modules
dist

# Local env files - never committed. Vite only exposes VITE_* to the client.
*.local

# Playwright
test-results
playwright-report

# Local agent tooling - vendored skills and MCP server config
.agents
.mcp.json
skills-lock.json
